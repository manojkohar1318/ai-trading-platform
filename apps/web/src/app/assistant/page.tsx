'use client';
import {useState} from 'react'; import Terminal from '@/components/Terminal';
export default function Assistant(){const [q,setQ]=useState('Analyze RELIANCE'); const [answer,setAnswer]=useState(''); const send=()=>setAnswer('DEMO AI response: quantitative analysis is available through the API foundation. Live market data and an external LLM provider are not configured yet.'); return <Terminal title="AI Assistant"><div className="max-w-4xl"><h1 className="text-2xl font-semibold">Trading AI</h1><p className="muted mt-1 text-sm">Intent → data retrieval → quant engine → risk engine → LLM response.</p><div className="card mt-5 p-4 min-h-[420px] flex flex-col"><div className="flex-1">{answer&&<div className="max-w-2xl bg-[#101722] border border-[#243047] rounded-xl p-4 text-sm leading-6">{answer}</div>} {!answer&&<div className="h-full grid place-items-center text-center"><div><div className="text-lg">Ask the terminal</div><div className="muted text-sm mt-2">“Which stocks are strongest?” · “Scan the market.” · “What is the trend?”</div></div></div>}</div><div className="flex gap-2 mt-4"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} className="flex-1 bg-[#0b1019] border border-[#263143] rounded-lg px-4 py-3 text-sm outline-none focus:border-[#6577c5]"/><button onClick={send} className="px-5 rounded-lg bg-[#6879d6] text-white text-sm font-medium">Analyze</button></div></div></div></Terminal>}
